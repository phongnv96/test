enum ThemeMode {
  LIGHT = 'light',
  DARK = 'dark',
}
enum ThemeColorSelected {
  successSelected = 'success',
  secondarySelected = 'secondary',
  primarySelected = 'primary',
}

export { ThemeColorSelected, ThemeMode }
